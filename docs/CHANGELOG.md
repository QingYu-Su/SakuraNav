# 更新日志

## [1.6.0] - 2026-05-03

### ✨ 新功能

- **网站备忘便签** — 为网站添加备注和待办列表，Todo 项在卡片上显示角标，支持 AI 可读开关
- **AI 工作流助手** — 新增 AI 工作流助手功能，AI 辅助自动化任务
- **关联推荐** — 网站卡片支持关联推荐，基于 AI 分析推荐相关站点，推荐上下文参与站内搜索
- **访问控制规则** — 新增备选 URL、右键菜单、互斥悬浮窗等访问控制规则
- **右键菜单** — 网站卡片支持右键快捷操作菜单
- **备选 URL** — 为网站设置备选访问地址，支持条件自动切换
- **数据导出/导入增强** — 支持仅网站卡片导出/导入、HMAC 签名校验、AI 分析并移除重复项
- **标签编辑双 Tab** — 标签编辑弹窗改为基本信息与关联网站双 Tab 布局，支持关联网站绑定与撤回
- **站点级在线检测** — 在线检测从全局设置改为站点级别配置，支持超时时间、判定模式、连续失败阈值
- **重复 URL 检测** — 创建网站时自动检测重复 URL 并提示
- **重复卡片增强** — 增强重复卡片提示，支持直接编辑、删除和跳转到已有卡片
- **快速搜索过滤** — 用户管理、标签关联网站、书签导入列表均支持快速搜索过滤
- **用户名密码校验增强** — 增强用户名和密码的格式校验规则
- **自定义 Tooltip** — 新增 Tooltip 组件替代原生 title 属性，悬浮提示体验更佳

### 🐛 Bug 修复

- **撤销失败修复** — 修复标签与网站同时删除时撤销失败的问题，统一撤销系统可扩展性约束
- **搜索闪烁修复** — 修复搜索过程中闪烁无关卡片，统一搜索结果卡片样式
- **图标选中状态** — 修复切换 Tab 后图标选中状态丢失
- **Tooltip 定位失效** — 修复 Tooltip 组件因 display:contents 导致定位失效
- **备选 URL 开关** — 修复备选 URL 开关逻辑，优化拖拽和菜单交互

### 🔧 改进

- **前端架构重构** — 主应用组件拆分为 Composition Root 架构，消除 props drilling，提升可维护性
- **统一 AI 分析入口** — 统一 AI 分析入口，移除废弃关联分析 API
- **移除 AI 助手浮动按钮** — 移除主页右下角 AI 助手浮动按钮
- **搜索性能优化** — 优化首次搜索性能
- **卡片匹配策略** — 统一数据导入中的卡片匹配策略，动态化导入构建，增强可扩展性

## [1.5.5] - 2026-04-27

### ✨ 新功能

- **多用户注册登录** — 支持多用户注册与登录，每位用户拥有独立的数据空间
- **用户数据隔离** — 标签、网站、外观配置等数据按用户隔离，管理员与游客共享公开数据
- **切换用户** — 新增切换用户弹窗，支持免密码快速切换、添加/删除用户，提供专用注册页
- **个人空间** — 新增个人空间页面，支持用户资料管理、头像上传和密码修改
- **注销账号** — 支持用户自主注销账号，彻底删除个人数据
- **OAuth 第三方登录** — 实现 OAuth 第三方登录系统，支持 GitHub、微信、企业微信、飞书、钉钉
- **第三方账号绑定** — 个人空间支持绑定/解绑第三方账号，切换用户弹窗支持第三方登录
- **OAuth 管理面板** — 管理面板新增 OAuth 配置管理，支持在线配置和连通性测试
- **AI 模型在线配置** — AI 模型配置从 config.yml 迁移至数据库，支持设置面板在线管理
- **AI 供应商+模型两级选择** — AI 配置改为供应商+模型两级下拉选择，支持多供应商
- **站点设置统一保存** — 系统设置统一全局保存，Tab 权限颜色区分，保存成功动画反馈
- **清除数据** — 新增"清除数据"功能，支持清除当前用户的标签和卡片
- **导入流程优化** — 重构外部文件导入流程，支持重复站点导入与重复检测提示
- **磨砂效果连续渐变** — 毛玻璃磨砂效果改为 0-100 连续渐变，修复桌面/移动端参数串扰
- **管理员引导初始化** — 管理员账户从 config.yml 迁移到数据库，首次运行引导设置
- **会话失效检测** — 添加会话过期检测与独立弹窗提示，提升多端登录体验

### 🐛 Bug 修复

- **OAuth 回调地址修复** — 修复 OAuth 回调重定向到 0.0.0.0 导致登录失败
- **OAuth 回调地址不匹配** — 修复回调地址与第三方平台注册地址不一致导致登录失败
- **GitHub 测试连通性** — 修复 GitHub OAuth 测试连通性 404 问题
- **OAuth 绑定流程** — 修复 OAuth 绑定时创建新用户、解绑弹窗不关闭和用户名变更后会话失效
- **OAuth 用户密码设置** — 修复 OAuth 用户首次设置密码与修改用户名流程
- **切换用户第三方登录** — 修复切换用户弹窗中第三方登录绑定到当前账号而非登录新账号
- **第三方登录选项实时更新** — 修复切换用户弹窗中第三方登录选项需要刷新页面才生效
- **密码输入框冲突** — 修复浏览器自动填充按钮与密码输入框的冲突，用户管理操作增加确认弹窗
- **多用户体验问题** — 修复多用户场景下 9 项体验问题（会话、权限、数据隔离等）
- **Logo/Favicon 上传裁剪** — 修复 Logo 和 Favicon 上传裁剪、资源清理和删除同步问题
- **资源延迟清理** — 删除网站卡片时延迟清理自定义图标资源，避免图标丢失
- **个人空间弹窗优化** — 加深弹窗颜色提高可读性，替换 alert 为自建弹窗，绑定/解绑流程优化

### 🔧 改进

- **统一权限体系** — 移除超级用户角色，统一为管理员权限体系
- **存储目录整理** — 将 Node.js localStorage 文件移入 storage/database 子目录

## [1.5.0] - 2026-04-22

### ✨ 新功能

- **统一设置弹窗** — 外观管理和系统设置合并为统一弹窗，操作更集中
- **可配置悬浮按钮** — 支持自定义右下角悬浮操作按钮
- **标签删除弹窗** — 删除标签前弹出确认弹窗，避免误操作
- **标签删除恢复排序** — 删除标签时自动恢复相关站点的排序位置
- **标签栏交互重构** — 侧边栏标签操作体验全面优化
- **卡片头部重构** — 编辑按钮移至左上角、删除按钮移至右上角、拖拽条居中，布局更清晰
- **标签溢出截断** — 网站卡片标签栏超出时自动计算可显示数量，末尾显示"..."
- **卡片类型装饰** — 卡片右下角增加网站/社交类型标识 Logo
- **标题栏操作按钮** — 新建标签/新建卡片按钮移至标题两侧，带磨砂效果
- **搜索引擎编辑按钮** — 编辑模式下自动在搜索栏内部左侧显示

### 🐛 修复

- **关闭弹窗丢弃分析结果** — 关闭设置弹窗时自动丢弃 AI 分析结果，不再弹出结果弹窗
- **拖拽预览隐藏按钮** — 拖拽卡片时编辑和删除按钮同步隐藏
- **拖拽手柄触摸区域** — 扩大拖拽横条的触摸区域，操作更友好
- **在线指示器间距** — 网站图标下方在线检查标志下移，视觉更舒适

## [1.4.5] - 2026-04-21

### ✨ 新功能

- **统一卡片创建入口** — 新建卡片时弹出类型选择器，可快速选择创建网站卡片或社交卡片
- **社交卡片扩展** — 新增 Telegram、小红书、抖音、QQ 群、企业微信、微信公众号六种卡片类型，共计 12 种
- **通用卡片详情页** — 所有社交卡片支持独立详情页展示
- **书签导入** — 支持导入浏览器书签文件（HTML），可逐条编辑后批量添加
- **三种导入模式** — 配置导入支持全量覆盖、增量合并、书签导入三种模式
- **ZIP 导入导出** — 配置导入导出改为 ZIP 格式，完整打包 storage 目录
- **Favicon 圆角处理** — 浏览器标签页 Favicon 自动裁剪为圆角，外观管理面板预览同步圆角
- **404 页面** — 访问不存在的路径时展示专用 404 页面
- **桌面端独立滚动** — 桌面端顶部导航栏固定不随内容滚动，侧边栏标签支持独立滚动条
- **悬浮按钮提示** — 鼠标悬浮右下角按钮时显示功能描述提示
- **反馈入口** — 右下角按钮改为问题反馈，点击跳转 GitHub Issues 页面
- **AI 站点检测配置** — 新增 AI 站点检测配置接口

## [1.4.0] - 2026-04-19

### ✨ 新功能

- **社交卡片系统** — 全新社交卡片功能，在导航页展示个人社交账号/联系方式
- **六种卡片类型** — 支持 QQ、微信、邮箱、B站、GitHub、博客六种社交卡片类型
- **QQ/微信详情页** — QQ 和微信卡片支持独立详情页，展示账号信息和二维码
- **自定义提示文字** — 每张社交卡片可自定义提示文字，留空则不显示
- **卡片拖拽排序** — 社交卡片支持拖拽排序，与普通网站卡片统一管理
- **悬浮弹窗** — 网站卡片支持鼠标悬浮弹出描述和标签信息
- **标签描述悬浮** — 侧边栏标签卡片支持悬浮显示完整描述
- **社交卡片标签自定义描述** — 支持为"社交卡片"虚拟标签设置自定义描述文案
- **保留标签名保护** — 系统保留标签（全部网站、社交卡片）不可被用户重命名覆盖

### 🐛 Bug 修复

- **修复网站描述为空时无法创建的问题** — 网站 `description` 字段允许空值存储

## [1.3.5] - 2026-04-18

### ✨ 新功能

- **AI 推荐标签自动关联** — AI 分析后勾选推荐的新标签，保存网站时自动创建标签并建立关联
- **单站点跳过在线检测** — 支持为单个网站设置跳过在线检测，适用于内网地址或不稳定的站点
- **图片裁剪** — 上传壁纸、Logo、Favicon 等图片时支持裁剪，精确控制显示区域
- **磨砂效果独立控制** — 毛玻璃卡片效果与壁纸解耦，桌面端和移动端可独立开关，无壁纸时也可使用
- **外观管理面板明亮主题** — 外观设置面板完整适配明亮模式，深色/浅色主题下均有良好的视觉体验

### 🐛 Bug 修复

- **修复 URL 协议补全** — 输入不带 `http://` 或 `https://` 前缀的网址时，自动补全为 `https://`，避免创建失败

### 🎨 UI 优化

- **图标选择器圆角更新** — 统一图标选择器圆角样式，视觉更加协调

## [1.3.0] - 2026-04-18

### ✨ 新功能

- **编辑器控制台** - 新增独立的 `/editor` 管理后台页面，支持批量管理所有标签和网站
- **配置服务层** - 新增 `ConfigService` 统一管理配置的读取、缓存和更新
- **搜索引擎配置持久化** - 搜索引擎配置通过 `useSearchEngineConfig` Hook 管理，支持 localStorage 持久化
- **站点名称 Hook** - 新增 `useSiteName` Hook，支持站点名称自定义和防抖保存
- **网站/标签编辑器 Hook** - 新增 `useSiteTagEditor` Hook，封装网站编辑器内的标签管理逻辑
- **Toast 通知 Hook** - 新增 `useToastNotify` Hook，统一管理全局 Toast 通知

### 🎨 UI 优化

- **搜索栏明亮模式适配** - 站内搜索按钮、搜索引擎编辑按钮、搜索提交按钮、清除按钮在明亮模式下使用深色文字和适当的背景色，提升可见性
- **图标选择器样式修复** - 修复图标选择器输入框样式问题

### 🔧 改进

- **lib 目录重构** - 将 `lib/` 目录重新组织为 `base/`、`config/`、`database/`、`services/`、`utils/` 子目录，职责更清晰
- **组件目录重组** - 将组件按功能分为 `sakura-nav/`（导航页）、`admin/`（管理后台）、`dialogs/`（弹窗）、`ui/`（通用 UI）目录
- **主应用组件拆分** - 将 `sakura-nav-app` 拆分为 `SearchBarSection`、`SiteContentArea`、`ContentTitleBar`、`SidebarTags`、`AppHeader` 等独立组件
- **编辑器控制台拆分** - 将编辑器控制台逻辑提取为 `useEditorConsole` Hook 和 `EditorSitesTab`、`EditorTagsTab` 等子组件
- **图标选择组件提取** - 将图标选择功能提取为独立的 `IconPicker` 组件
- **图标工具函数提取** - 将图标相关工具函数提取到 `icon-utils.ts` 公共模块
- **外观工具函数提取** - 将外观配置处理逻辑提取到 `appearance-utils.ts` 公共模块
- **搜索栏样式函数提取** - 提取搜索栏按钮样式为 `style-helpers.ts` 中的纯函数，支持主题感知
- **数据库操作迁移至服务层** - 将数据库操作从 `db.ts`（1800+ 行）迁移至 Repository 模式，代码结构更清晰
- **搜索服务独立** - 将搜索建议逻辑提取为 `SearchService`
- **CHANGELOG 归档** - 将 `CHANGELOG.md` 移至 `docs/` 目录，与开发文档统一管理

## [1.2.0] - 2026-04-17

### ✨ 新功能

- **自定义搜索引擎配置** - 新增搜索引擎管理面板，支持增删改搜索引擎，可配置名称、搜索地址、图标和卡片颜色
- **搜索引擎图标系统** - 支持文字图标（首字符自动生成）、官方 Favicon 自动获取、本地上传/远程 URL 上传自定义图标
- **搜索栏清空按钮** - 搜索栏输入框左侧替换为 X 清空按钮，可清除输入并关闭搜索建议下拉框
- **搜索建议关闭** - 搜索建议下拉框顶部新增「收起搜索建议」固定项，支持手动关闭
- **AI 智能推荐** - 搜索栏新增 AI 智能推荐功能，输入关键词可获取相关网站推荐
- **网站在线检测** - 管理面板新增网站在线状态批量检测功能
- **搜索栏 Tab 切换提示** - 搜索栏占位文字提示用户按 Tab 键可切换搜索引擎
- **空输入搜索引擎跳转** - 搜索栏无内容时点击搜索按钮可跳转到搜索引擎首页

### 🎨 UI 优化

- **搜索引擎下拉框** - 移除「当前」提示文字，下拉框宽度与引擎卡片一致
- **搜索引擎编辑弹窗** - 编辑/新增使用独立弹窗，不影响列表布局
- **颜色选择器** - 自定义颜色按钮使用彩虹渐变样式，选中时外圈高亮
- **图标选择交互** - 图标选项支持选中态高亮和 Check 标记，上传后自动选中
- **无内容按钮禁用态** - 搜索栏清空按钮和站内搜索按钮在无内容时显示禁用状态

### 🐛 Bug 修复

- **修复搜索引擎配置不持久化** - 搜索引擎配置通过 localStorage 持久化，重启项目后配置不丢失
- **修复数据库迁移残余表问题** - 清理残余表并修复 `updated_at` 字段转换

### 🔧 改进

- **搜索栏逻辑重构** - 抽取搜索栏状态管理到 `useSearchBar` 自定义 Hook，主页和浮动搜索栏共用逻辑

## [1.1.5] - 2026-04-16

### ✨ 新功能

- **AI 智能分析** - 编辑网站时新增「AI 分析」按钮，自动识别网站标题、描述、关联标签和推荐新标签，支持配置自定义 AI 模型
- **网站编辑器内嵌标签管理** - 编辑网站时可直接新建/编辑标签，无需跳转到标签管理页面

### 🎨 UI 优化

- **标签栏精简** - 移除标签卡片上的图标显示，标签编辑仅需名称和描述，侧边栏更窄更简洁
- **标签栏居中排版** - 标签卡片标题和描述居中对齐，支持自动换行，编辑模式下侧边栏自动延展显示编辑按钮
- **拖拽手柄优化** - 标签卡片和网站卡片的拖拽按钮替换为顶部细横线，更轻量优雅
- **网站卡片图标强制正方形** - 图标容器使用固定尺寸 + overflow-hidden，确保所有图标均为正方形
- **编辑按钮样式优化** - 光明模式下网站卡片和标签卡片的编辑按钮颜色加深，提升可见性

### 🐛 Bug 修复

- **修复网站描述为空时创建失败** - 数据库 `description` 列移除 NOT NULL 约束，支持无描述创建网站
- **修复编辑按钮触发页面跳转** - 点击网站卡片编辑按钮时不再冒泡触发 `<a>` 标签导航
- **修复拖拽时卡片尺寸变化** - 拖拽 overlay 卡片锁定原始宽高，无标签网站拖拽时不再缩小
- **修复官方图标获取失败仍显示** - favicon.im 返回 1x1 空白图或 404 时不显示「官方图标」选项
- **修复 AI 分析后图标选择逻辑** - AI 分析完成后优先选择官方图标，不可用时自动选择文字图标
- **修复站点名称修改不生效** - 修改站点名称后浏览器标签标题实时更新，并增加防抖保存

## [1.1.0] - 2026-04-16

### ✨ 新功能

- **站点名称自定义** - 在「其他」配置面板中新增站点名称设置，支持最多 30 字符，自动保存，实时更新浏览器标签标题和导航栏显示名称
- **网站卡片图标背景色** - 编辑网站时可为图标设置自定义背景色，支持文字图标和官方图标的背景色定制
- **文字图标** - 编辑网站时新增「文字图标」选项，自动取网站名首字生成图标，支持自定义背景色
- **标签 Logo 自定义** - 标签栏支持为每个分类标签设置独立的 Logo 图标
- **网站编辑器图标交互重构** - 图标选择改为可视化选项卡模式，支持「当前图标 / 文字图标 / 官方图标 / 上传图标」快速切换
- **标签切换过渡动画** - 切换标签时新增类 pjax 过渡效果（缩放 + 模糊 + 透明度），顶部进度条 sweep 动画，新内容卡片依次入场
- **官方图标动态加载** - 选择「官方图标」时不再下载到服务器本地，直接使用 favicon.im 动态 URL，节省服务器存储空间且图标始终为最新
- **图标加载失败 Fallback** - 动态加载的官方图标加载失败时自动降级显示网站首字母

### 🐛 Bug 修复

- **修复 npm install 报错** - 修正 package.json 依赖配置，解决安装失败问题
- **修复 Docker 读取配置文件失败** - 修正 docker-entrypoint.sh 中配置文件路径和值解析逻辑

### 🔧 改进

- **标签取消收起逻辑** - 移除分类标签的收起/折叠行为，标签始终完整展示
- **网站编辑器 UI 优化** - 图标编辑区域重新设计，操作流程更直观

## [1.0.5] - 2025-04-02

### 🐛 Bug 修复

- **修复配置文件修改后不生效的问题** - 重启项目后 `admin.path`、`admin.username`、`admin.password` 等配置无法生效（`server-config.ts`）
- **修复自定义登录路径 404 的问题** - 当 `path` 值为纯数字时（如 `5201314`），YAML 会将其解析为数字类型，导致与字符串比较失败（`server-config.ts`）
- **修复构建失败时日志被截断的问题** - `build-and-run.js` 在 lint 或 build 出错时只输出部分错误信息，甚至不输出原因
- **修复 Ctrl+C 强制退出后 `.next` 目录被锁定的问题** - 终止服务后 `.next/standalone` 目录仍被 Node 进程占用，导致重新构建时报 `EBUSY` 错误（`build-and-run.js`）

### 🔧 改进

- **配置文件格式优化** - `config.example.yml` 中用户名、密码、登录路径等参数使用双引号包裹，避免 YAML 类型推断问题
- **Docker 入口脚本改进** - `docker-entrypoint.sh` 正确提取双引号包裹的配置值，新增 `path` 字段支持
- **动态路由强制刷新** - 为 `[...slug]` 路由添加 `force-dynamic`，确保配置变更实时生效（`src/app/[...slug]/page.tsx`）
- **构建阶段自动禁用日志** - 构建过程中自动关闭日志文件输出，避免构建报错（`src/lib/logger.ts`）

### 📝 文档

- 更新中文 README 和英文 README 中的配置示例，使用双引号包裹配置值

## [1.0.0] - 2025-04-01

### 🎉 首个正式版本发布

SakuraNav 是一个基于 Next.js + React + TypeScript + SQLite 的优雅个人导航页。

### 界面体验

- **响应式设计** - 完美适配桌面端和移动端，自适应布局
- **明暗主题** - 支持明亮 / 暗黑主题切换，支持跟随系统
- **主题定制** - 明暗主题分别配置壁纸、字体、透明度、文字颜色
- **动态背景** - 明亮模式飘落樱花花瓣（80 个），暗黑模式闪烁星星（30 颗）
- **毛玻璃卡片** - 桌面端和移动端可独立开关卡片毛玻璃效果
- **桌面/移动壁纸分离** - 桌面端和移动端可分别设置不同壁纸
- **三种字体预设** - Space Grotesk（科技感）、Noto Serif SC（杂志感）、Noto Sans SC（日常清晰）
- **Logo / Favicon 自定义** - 支持上传自定义 Logo 和 Favicon
- **渐进式加载** - 默认"全部网站"模式和标签视图下均支持无限滚动分页加载（每页 12 条）

### 登录管理

- **隐藏登录路由** - 自定义登录入口路径（`admin.path`），游客无法发现管理入口
- **单用户登录** - 支持 30 天免登录，记住登录状态
- **管理员二次确认** - 执行敏感操作时需输入密码确认
- **权限控制** - 登录后可见隐藏标签与隐藏站点
- **编辑器控制台** - 独立的 `/editor` 管理后台页面

### 标签管理

- **标签分类** - 网站按标签分类展示，支持标签 Logo 设置
- **拖拽排序** - 登录后支持拖拽调整标签顺序
- **隐藏标签** - 可设置隐藏标签，仅登录后可见
- **侧边栏折叠** - 移动端侧边栏自动折叠

### 网站管理

- **网站增删改** - 登录后可管理网站信息
- **拖拽排序** - 登录后在单个标签下拖拽网站顺序
- **网站置顶** - 支持网站置顶显示（`is_pinned`）
- **批量管理** - 支持网站与多个标签关联
- **批量编辑** - 编辑器控制台支持批量管理所有标签和网站

### 搜索功能

- **多引擎支持** - Google / Baidu / 站内搜索切换
- **站内搜索** - 在当前视图范围内搜索站点名、描述、标签
- **搜索建议** - 浮动搜索弹窗，支持实时搜索建议
- **键盘导航** - 搜索建议支持上下方向键选择、Enter 确认、Escape 关闭

### 数据管理

- **壁纸上传** - 本地上传壁纸，或通过 URL 下载
- **配置导入导出** - 支持配置数据的导入和导出（ZIP 格式，含数据库与资源文件）
- **配置重置** - 支持一键重置到默认配置
- **示例数据** - 首次运行自动创建示例标签、网站和默认主题配置

### 技术特性

- **全栈 TypeScript** - 前后端统一类型安全
- **React 19** - 使用最新 React 特性（useEffectEvent、useDeferredValue、useTransition）
- **React Compiler** - 启用 React Compiler 自动优化渲染性能
- **Repository Pattern** - 数据访问层采用 Repository 模式封装
- **自动数据库迁移** - SQLite 表结构变化自动检测和迁移
- **WAL 模式** - SQLite 启用 Write-Ahead Logging 提升并发性能
- **JWT 认证** - 基于 jose 库的 JWT + HttpOnly Cookie 安全认证
- **Server-Only Config** - 敏感配置使用 server-only 保护，仅在服务端可访问
- **日志系统** - 自定义日志记录器，支持文件输出和 30 天自动保留
- **Docker 部署** - 多阶段构建，非 root 用户运行，standalone 输出模式
- **YAML 配置** - 外部 YAML 配置文件，支持自定义端口和管理员凭据
